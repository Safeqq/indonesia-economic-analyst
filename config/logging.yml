version: 1
disable_existing_loggers: false
handlers:
  console:
    class: logging.StreamHandler
    level: INFO
    formatter: json
    stream: ext://sys.stdout
formatters:
  json:
    (): pipelines.utils.structured_logging.JsonFormatter
root:
  level: INFO
  handlers: [console]
